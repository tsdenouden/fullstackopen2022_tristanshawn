{
  "name": "phonebookbackend",
  "version": "1.0.0",
  "description": "backend for phonebook app in express",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../phonebookFrontend && npm run build && cp -r build ../phonebookBackend",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tristan Shawn den Ouden",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^6.6.7",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
